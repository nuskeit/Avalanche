import { TypeDef_DTO } from '../src/core/avalanche-app/root-diagram/diagram/element/field/type-def/data/type-def-dto'
import { DataType } from '../src/core/general/domain'
import { Reviver } from '../src/core/factories/data-transformation-services/application/reviver-factory'

describe('test AppFactory', () => {
	test('createTypeDef', () => {
		// const typeDefDto = new TypeDef_DTO("new-key", "new-name", DataType.string, null)
		// const reviver = new Reviver()
		// reviver.createTypeDef(
		// expect(typeDefDto.fallbackDataType).toBe(DataType.string)
	})
})